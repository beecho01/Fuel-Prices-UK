# Development dependencies for local type checking / IDE support.
# These packages are provided by the Home Assistant runtime environment;
# they are listed here only so editors/Pyright can resolve imports locally.
homeassistant>=2024.1.0
aiohttp>=3.9.0
geopy>=2.4.0
voluptuous>=0.13.1
